import prompt from 'prompt-sync'
const input = prompt()
function main(){
    let [l, c, perc] = input('> ').split(' ').map(Number) 
    let area_casa, area_terreno, lado_terreno
    while(l !== 0){
        area_casa = l * c
        area_terreno = (area_casa / percentual) * 100
        lado_terreno = 

        [l, c, perc] = input('> ').split(' ').map(Number)
    }

}


main()